PORT=3000

# Not used yet — uncomment/add as your case study needs a database, mail,
# or a job queue. Values match docker-compose.yml's services.
# DATABASE_URL=mysql://root:root@localhost:3306/student_app
# MAIL_HOST=localhost
# MAIL_PORT=1025
# REDIS_HOST=localhost
# REDIS_PORT=6379
